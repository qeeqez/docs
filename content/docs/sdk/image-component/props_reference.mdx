---
title: "Props Reference"
description: "Complete API reference for all Image component props and TypeScript interfaces"
---

import { Callout } from 'fumadocs-ui/components/callout'

## Core Props

### `id` **(Required)**

**Type:** `string`

The unique image identifier from your RIXL dashboard. Upload your image to [dash.rixl.com](https://dash.rixl.com) to get this ID.

```tsx
<Image id="your-image-id" />
```


<Callout type="info">
    Good alt text is descriptive but concise. Avoid "image of" or "picture of" - screen readers already announce it's an image.
</Callout>

### `className` **(Optional)**

**Type:** `string` | **Default:** `undefined`

CSS classes for styling the image container. Supports all CSS frameworks including TailwindCSS.

```tsx
<Image
  id="your-image-id"
  className="object-contain w-full h-full rounded-lg shadow-md"
/>
```