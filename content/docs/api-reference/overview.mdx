---
title: API Overview
description: Complete guide to the RIXL API endpoints and authentication.
---

import { Callout, Tabs, Tab } from '../../../components';

Build powerful media workflows with RIXL's RESTful API. Handle uploads, processing, and global delivery programmatically.

// TODO: Add API endpoint examples and response schemas.

<Callout type="info" title="Prerequisites">
  Set up [authentication](/docs/getting-started/authentication), [create a project](/docs/getting-started/create-project), and [generate API keys](/docs/account/api-access/keys).
</Callout>

### API Endpoint

```
https://api.rixl.com/v1
```

### Authentication

Include your project API key in the request header:

```
X-API-Key: YOUR_PROJECT_API_KEY_HERE
```

### Core Resources

**Projects** - Organize media and control access with project management. Create and manage projects for media organization and team access control.

**Media Upload** - Upload videos and images with automatic transcoding and format conversion. Process videos and images with intelligent optimization.

**Feeds** - Create structured content collections for dynamic delivery. Build social media-style experiences with organized content.

**Processing** - Monitor transcoding status and retrieve processed assets. Track video processing progress and access multiple quality renditions.

### Getting Started

1. **Authentication** - Generate an API key from your project settings
2. **Upload Media** - Use the upload endpoints to add videos and images
3. **Monitor Processing** - Track processing status for uploaded content
4. **Retrieve Assets** - Access processed media through CDN URLs
5. **Create Feeds** - Organize content into structured feeds for delivery

### Quick Start Examples

<Tabs items={['cURL', 'JavaScript', 'Python']}>
<Tab value="cURL">
```bash
# Initialize a video upload
curl -X POST https://api.rixl.com/v1/videos/upload/init \
  -H "X-API-Key: YOUR_PROJECT_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "file_name": "my-video.mp4",
    "video_quality": "shorts",
    "image_format": "jpg"
  }'

# Get all videos in your project
curl -X GET https://api.rixl.com/v1/videos \
  -H "X-API-Key: YOUR_PROJECT_API_KEY" \
  -G \
  -d "limit=25" \
  -d "offset=0"
```
</Tab>

<Tab value="JavaScript">
```javascript
// Initialize a video upload
const initResponse = await fetch('https://api.rixl.com/v1/videos/upload/init', {
  method: 'POST',
  headers: {
    'X-API-Key': 'YOUR_PROJECT_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    file_name: 'my-video.mp4',
    video_quality: 'shorts',
    image_format: 'jpg'
  })
});

const uploadData = await initResponse.json();
console.log(uploadData);
```
</Tab>

<Tab value="Python">
```python
import requests

# Initialize a video upload
response = requests.post(
    'https://api.rixl.com/v1/videos/upload/init',
    headers={
        'X-API-Key': 'YOUR_PROJECT_API_KEY',
        'Content-Type': 'application/json'
    },
    json={
        'file_name': 'my-video.mp4',
        'video_quality': 'shorts',
        'image_format': 'jpg'
    }
)

upload_data = response.json()
print(upload_data)
```
</Tab>
</Tabs>

### Response Format

All API responses use JSON format with consistent structure:

```json
{
  "status": "success",
  "data": { /* response data */ },
  "message": "Operation completed successfully"
}
```

**Video Upload Init Response:**
```json
{
  "video_id": "VI9VXQxWXQ",
  "video_presigned_url": "https://videos.rixl.com/...",
  "poster_id": "PS5IMKoFLm", 
  "poster_presigned_url": "https://images.rixl.com/...",
  "upload_expires": 1640995200
}
```

**Paginated Response (List Videos):**
```json
{
  "data": [
    {
      "id": "VI9VXQxWXQ",
      "duration": 30.5,
      "width": 1920,
      "height": 1080,
      "file": {
        "id": "FpvLwyDMqu",
        "name": "original.mp4",
        "size": 1048576,
        "url": "https://videos.rixl.com/Bq4y3QB38S/FpvLwyDMqu",
        "status": "ready"
      }
    }
  ],
  "pagination": {
    "limit": 25,
    "offset": 0,
    "total": 100
  }
}
```

### Error Handling

API errors include HTTP status codes and detailed error messages:

```json
{
  "code": 400,
  "error": "Invalid request body",
  "details": "The provided file format is not supported"
}
```

### Rate Limits

API requests are rate-limited based on your subscription plan:

| Plan | Requests per Hour | Burst Limit |
|------|-------------------|-------------|
| Free | 100 | 10 per minute |
| Pro | 1,000 | 50 per minute |
| Enterprise | Custom | Custom |

<Callout type="warning">
  Each subscription plan has its own API call limits. Check your usage in the dashboard to avoid interruptions.
</Callout>

### Status Codes

| Code | Meaning |
|------|---------|
| 200 | Success |
| 201 | Created |
| 400 | Bad Request |
| 401 | Unauthorized |
| 403 | Forbidden |
| 404 | Not Found |
| 429 | Rate Limited |
| 500 | Server Error |

### SDKs and Libraries

The RIXL API uses standard REST principles, making it compatible with any HTTP client library:

- **cURL** for command-line testing
- **Fetch API** for JavaScript/Node.js
- **Requests** for Python
- **HTTPClient** for Go
- **OkHttp** for Java

### Support

If you encounter issues while using the API:

- **Documentation** - Complete guides and examples
- **Email** - Direct support at [api@rixl.com](mailto:api@rixl.com)
- **Status Page** - Platform updates at [status.rixl.com](https://status.rixl.com)

<Callout type="info">
  For the latest API documentation and testing, visit [api.rixl.com/docs](https://api.rixl.com/docs).
</Callout>
