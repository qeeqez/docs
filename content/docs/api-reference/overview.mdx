---
title: API Reference
description: Complete API documentation for integrating RIXL into your applications.
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Card, Cards } from 'fumadocs-ui/components/card'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'

With the RIXL API, you can access all platform features and easily integrate them into your applications.

<Callout type="info">

Before you start, make sure you have [set up authentication](/docs/getting-started/authentication)and [create a project](/docs/getting-started/create-project) by following these guides.
</Callout>

## Base URL

Use this base URL for all your API requests:

```
https://api.rixl.com/v1
```

## Authentication

Add your API key to the request header:

```bash
X-API-Key: YOUR_PROJECT_API_KEY
```

## Core Resources

<Cards>
  <Card title="Projects" description="Manage projects and organizational structure">
You can create, set up, and manage projects to organize your media and control who can access each project.
  </Card>
  
  <Card title="Media Upload" description="Upload and process videos and images">
    Upload your media with automatic optimization, transcoding, and format conversion for optimal project delivery.
  </Card>
  
  <Card title="Feeds" description="Create dynamic content collections">
Organize your media into feeds that you can embed or use for dynamic project content sharing.
  </Card>
  
  <Card title="Team Management" description="Manage team members and permissions">
Invite your team, assign roles, and manage who can access your projects and media.
  </Card>
</Cards>

## Quick Start Example

Here are some examples of how to use the RIXL API with the right endpoints:

<Tabs items={['cURL', 'JavaScript', 'Python']}>
<Tab value="cURL">
```bash
# Initialize a video upload
curl -X POST https://api.rixl.com/videos/upload/init \
  -H "X-API-KEY: YOUR_PROJECT_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "file_name": "my-video.mp4",
    "video_quality": "shorts",
    "image_format": "jpg"
  }'

# Get all videos in your project
curl -X GET https://api.rixl.com/videos \
  -H "X-API-KEY: YOUR_PROJECT_API_KEY" \
  -G \
  -d "limit=25" \
  -d "offset=0"

# Get a specific video
curl -X GET https://api.rixl.com/videos/VI9VXQxWXQ \
  -H "X-API-KEY: YOUR_PROJECT_API_KEY"
```
</Tab>

<Tab value="JavaScript">
```javascript
// Initialize a video upload
const initResponse = await fetch('https://api.rixl.com/videos/upload/init', {
  method: 'POST',
  headers: {
    'X-API-KEY': 'YOUR_PROJECT_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    file_name: 'my-video.mp4',
    video_quality: 'shorts',
    image_format: 'jpg'
  })
});

const uploadData = await initResponse.json();
console.log(uploadData);

// Get all videos in your project
const videosResponse = await fetch('https://api.rixl.com/videos?limit=25&offset=0', {
  headers: {
    'X-API-KEY': 'YOUR_PROJECT_API_KEY'
  }
});

const videos = await videosResponse.json();
console.log(videos);
```
</Tab>

<Tab value="Python">
```python
import requests

# Initialize a video upload
response = requests.post(
    'https://api.rixl.com/videos/upload/init',
    headers={
        'X-API-KEY': 'YOUR_PROJECT_API_KEY',
        'Content-Type': 'application/json'
    },
    json={
        'file_name': 'my-video.mp4',
        'video_quality': 'shorts',
        'image_format': 'jpg'
    }
)

upload_data = response.json()
print(upload_data)

# Get all videos in your project
videos_response = requests.get(
    'https://api.rixl.com/videos',
    headers={'X-API-KEY': 'YOUR_PROJECT_API_KEY'},
    params={'limit': 25, 'offset': 0}
)

videos = videos_response.json()
print(videos)
```
</Tab>
</Tabs>

## Response Format

API responses follow standard REST patterns:

**Successful Response (Video Upload Init):**
```json
{
  "video_id": "VI9VXQxWXQ",
  "video_presigned_url": "https://videos.rixl.com/Bq4y3QB38S/VI9VXQxWXQ?X-Amz-Algorithm=...",
  "poster_id": "PS5IMKoFLm", 
  "poster_presigned_url": "https://images.rixl.com/Bq4y3QB38S/PS5IMKoFLm?X-Amz-Algorithm=...",
  "upload_expires": 1640995200
}
```

**Paginated Response (List Videos):**
```json
{
  "data": [
    {
      "id": "VI9VXQxWXQ",
      "duration": 30.5,
      "width": 1920,
      "height": 1080,
      "file": {
        "id": "FpvLwyDMqu",
        "name": "original.mp4",
        "size": 1048576,
        "url": "https://videos.rixl.com/Bq4y3QB38S/FpvLwyDMqu",
        "status": "ready"
      }
    }
  ],
  "pagination": {
    "limit": 25,
    "offset": 0,
    "total": 100
  }
}
```

## Error Handling

If there is an error, the response will use the ResponseError format:

```json
{
  "code": 400,
  "error": "Invalid request body",
  "details": "The provided file format is not supported"
}
```

## Rate Limits

<Callout type="warning">
Each subscription plan has its own API call limits. If you reach your limit, new requests will be denied until your usage resets. You can check your usage in your dashboard to avoid any interruptions.
</Callout>

| Plan | Requests per Hour | Burst Limit |
|------|-------------------|-------------|
| Free | 100 | 10 per minute |
| Pro | 1,000 | 50 per minute |
| Enterprise | Custom | Custom |

## Status Codes

| Code | Meaning |
|------|---------|
| 200 | Success |
| 201 | Created |
| 400 | Bad Request |
| 401 | Unauthorized |
| 403 | Forbidden |
| 404 | Not Found |
| 429 | Rate Limited |
| 500 | Server Error |

## SDKs and Libraries

<Callout type="info">
We are working on official SDKs for popular programming languages and will release them soon.
</Callout>

For now, feel free to use any HTTP client in your favorite programming language to connect to the API.

## Support

If you encounter issues or need clarification while using the API, support is available:

- **Documentation**: Complete guides and examples
- **Email**: For direct support, email us at [api@rixl.com](mailto:api@rixl.com)
- **Status Page**: For platform updates, visit [status.rixl.com](https://status.rixl.com)

<Callout type="tip">
For latest API info and testing, visit [api.rixl.com/docs](https://api.rixl.com/docs).
</Callout>
