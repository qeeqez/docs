---
title: Guides & Examples
description: Step-by-step tutorials, real-world examples, and practical implementation guides for building amazing media experiences with RIXL.
---

import {Callout, Card, Columns} from "@/components";
import {Tab, Tabs} from 'fumadocs-ui/components/tabs';

Welcome to the **RIXL Guides**—your comprehensive resource for building media-rich applications. Whether you're implementing video streaming, optimizing images, or creating dynamic content feeds, these guides provide practical, step-by-step instructions to help you succeed.

## Quick Actions

<Columns cols={3}>
  <Card
    title="React Integration"
    href="/sdk/getting-started/overview"
    icon="layers"
  >
    Drop-in components for video players, optimized images, and content feeds.
  </Card>
  <Card
    title="API Implementation"
    href="/platform/api-documentation/overview"
    icon="code"
  >
    Build custom workflows with our comprehensive REST API.
  </Card>
  <Card
    title="Dashboard Workflow"
    href="/platform/dashboard/guide"
    icon="monitor"
  >
    Upload, manage, and optimize media through our web interface.
  </Card>
</Columns>

## Implementation Guides

### Frontend Integration

<Columns cols={3}>
  <Card title="React Components" icon="layers">
    Video, Image, and Feed components with automatic optimization and global delivery.
  </Card>
  <Card title="JavaScript SDK" icon="code">
    Vanilla JavaScript integration for any web application or framework.
  </Card>
  <Card title="Mobile Integration" icon="smartphone">
    Responsive components that work seamlessly across all devices and screen sizes.
  </Card>
</Columns>

### Backend Integration

<Columns cols={3}>
  <Card title="Upload Workflows" icon="upload">
    Programmatic media uploads with progress tracking and status monitoring.
  </Card>
  <Card title="Processing Automation" icon="workflow">
    Automated media processing pipelines with quality tier selection.
  </Card>
  <Card title="Content Management" icon="folder-open">
    Organize and manage media libraries through API endpoints.
  </Card>
</Columns>

## Real-World Use Cases

### Video Applications

<Columns cols={3}>
  <Card title="Video Streaming Platform" icon="play">
    Build Netflix-style streaming with adaptive quality, HLS & DASH delivery, and global CDN.
  </Card>
  <Card title="Social Media App" icon="users">
    User-generated content with automatic optimization and mobile-first delivery.
  </Card>
  <Card title="E-learning Platform" icon="book-open">
    Educational content with progress tracking, quality controls, and accessibility features.
  </Card>
</Columns>

### Image Applications

<Columns cols={3}>
  <Card title="E-commerce Store" icon="shopping-cart">
    Product galleries with AVIF optimization, lazy loading, and responsive images.
  </Card>
  <Card title="Portfolio Website" icon="camera">
    High-quality image galleries with smart placeholders and instant loading.
  </Card>
  <Card title="Content Management" icon="file-text">
    Editorial workflows with automatic optimization and multi-format delivery.
  </Card>
</Columns>

### Content Feed Applications

<Columns cols={3}>
  <Card title="Social Feed" icon="activity">
    Instagram-style feeds with automatic updates and infinite scroll.
  </Card>
  <Card title="News Platform" icon="newspaper">
    Dynamic content feeds with real-time updates and personalization.
  </Card>
  <Card title="Creator Platform" icon="star">
    Multi-creator content feeds with subscription and monetization features.
  </Card>
</Columns>

## Code Examples

### React Components

<Tabs items={['Video Player', 'Image Gallery', 'Content Feed']}>
  <Tab value="Video Player">
    ```jsx
    import {Video, useProgressBar} from '@rixl/videosdk-react';
    import {useRef} from 'react';

    function VideoPlayer({videoId}) {
    const videoRef = useRef(null);
    const {progress, duration} = useProgressBar({video: videoRef.current});

    return (
    <div className="video-container">
    <Video
    ref={videoRef}
       id={videoId}
       className="w-full h-auto rounded-lg"
       progressBar={true}
       allowFullscreen={true}
       autoplay={false}
       muted={false}
  />
  <div className="video-info">
    <p>Progress: {Math.round(progress * 100)}%</p>
    <p>Duration: {duration}s</p>
  </div>
</div>
);
}
```
</Tab>

<Tab value="Image Gallery">
  ```jsx
  import {Image} from '@rixl/videosdk-react';

  function ImageGallery({images}) {
  return (
  <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
{images.map((image) => (
  <div key={image.id} className="aspect-square">
  <Image
    id={image.id}
    alt={image.description}
    className="w-full h-full object-cover rounded-lg hover:scale-105 transition-transform"
    loading="lazy"
  />
</div>
))}
</div>
);
}
```
</Tab>

<Tab value="Content Feed">
  ```jsx
  import {Feed} from '@rixl/videosdk-react';

  function ContentFeed({feedId, creatorId}) {
  return (
  <div className="max-w-2xl mx-auto">
  <Feed
  feedId={feedId}
     creatorId={creatorId}
     className="w-full space-y-6"
     showHeader={true}
     enableInfiniteScroll={true}
     itemsPerPage={10}
/>
</div>
);
}
```
</Tab>
</Tabs>

### API Integration Examples

<Tabs items={['Upload Video', 'Process Image', 'Get Feed Content']}>
  <Tab value="Upload Video">
    ```javascript
    // Initialize video upload
    const uploadVideo = async (file) => {
    const initResponse = await fetch('https://api.rixl.com/videos/upload/init', {
    method: 'POST',
    headers: {
    'X-API-Key': 'YOUR_PROJECT_API_KEY',
    'Content-Type': 'application/json'
  },
    body: JSON.stringify({
    file_name: file.name,
    video_quality: 'pro',
    image_format: 'jpg'
  })
  });

    const {video_id, video_presigned_url} = await initResponse.json();

    // Upload file to presigned URL
    await fetch(video_presigned_url, {
    method: 'PUT',
    body: file
  });

    return video_id;
  };
    ```
  </Tab>

  <Tab value="Process Image">
    ```javascript
    // Upload and process image
    const uploadImage = async (imageFile) => {
    // Initialize upload
    const initResponse = await fetch('https://api.rixl.com/images/upload/init', {
    method: 'POST',
    headers: {
    'X-API-Key': 'YOUR_PROJECT_API_KEY',
    'Content-Type': 'application/json'
  },
    body: JSON.stringify({
    name: imageFile.name,
    format: 'jpg'
  })
  });

    const {image_id, presigned_url} = await initResponse.json();

    // Upload file to presigned URL
    await fetch(presigned_url, {
    method: 'PUT',
    body: imageFile
  });

    // Complete upload
    const completeResponse = await fetch('https://api.rixl.com/images/upload/complete', {
    method: 'POST',
    headers: {
    'X-API-Key': 'YOUR_PROJECT_API_KEY',
    'Content-Type': 'application/json'
  },
    body: JSON.stringify({
    image_id: image_id
  })
  });

    const result = await completeResponse.json();
    return result.data;
  };
    ```
  </Tab>

  <Tab value="Get Feed Content">
    ```javascript
    // Get feed content
    const getFeedContent = async (feedId, creatorId = null) => {
    const params = new URLSearchParams();
    if (creatorId) params.append('creator_id', creatorId);

    const response = await fetch(`https://api.rixl.com/feeds/${feedId}?${params}`, {
    headers: {
    'X-API-Key': 'YOUR_PROJECT_API_KEY'
  }
  });

    const result = await response.json();
    return result.data;
  };
    ```
  </Tab>
</Tabs>

## Implementation Patterns

### Progressive Enhancement

<Columns cols={3}>
  <Card title="Start Simple" icon="play">
    Begin with basic Video and Image components, then add advanced features as needed.
  </Card>
  <Card title="Add Interactivity" icon="settings">
    Implement progress tracking, quality controls, and user preferences.
  </Card>
  <Card title="Scale Globally" icon="globe">
    Leverage CDN delivery and adaptive streaming for worldwide performance.
  </Card>
</Columns>

### Performance Optimization

<Columns cols={3}>
  <Card title="Lazy Loading" icon="zap">
    Load media only when needed to improve initial page performance.
  </Card>
  <Card title="Smart Caching" icon="database">
    Utilize browser caching and CDN edge locations for instant delivery.
  </Card>
  <Card title="Adaptive Quality" icon="wifi">
    Automatically adjust quality based on user connection and device capabilities.
  </Card>
</Columns>

## Next Steps

Ready to start building? Choose your path:

<Columns cols={3}>
  <Card
    title="React SDK Setup"
    href="/sdk/getting-started/overview"
    icon="layers"
  >
    Install and configure RIXL components in your React application.
  </Card>
  <Card
    title="API Integration"
    href="/platform/api-documentation/overview"
    icon="code"
  >
    Build custom workflows with our comprehensive REST API.
  </Card>
  <Card
    title="Dashboard Upload"
    href="/platform/dashboard/guide"
    icon="upload"
  >
    Start uploading and optimizing media through our web interface.
  </Card>
</Columns>

<div className="mt-12">
  <Callout type="tip" title="Need inspiration?">
    Explore our [Best Practices](/getting-started/best-practices) for optimization tips, check out the [Platform
    Documentation](/platform) for comprehensive features, or dive into the [API
    Reference](/platform/api-documentation/overview) for detailed technical documentation.
  </Callout>
</div>
